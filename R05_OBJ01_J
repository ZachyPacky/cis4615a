import java.io.*;
import java.util.*;
import java.math.*;
public class R05_OBJ01_J { //OBJ01-J. Limit accessibility of fields
    /*
    public class Widget {
        public int total; // Number of elements

        void add() {
            if (total < Integer.MAX_VALUE) {
            total++;
            // ...
            } else {
                throw new ArithmeticException("Overflow");
            }
        }

        void remove() {
            if (total > 0) {
                total--;
                // ...
            } else {
                throw new ArithmeticException("Overflow");
            }
        }
    }
     */

    private static int total; // Declared private

    public static int getTotal () {
        return total;
    }

    // Definitions for add() and remove() remain the same

    public static void main(String []args) {
        int a = 0;
        a = getTotal();
    }
}
